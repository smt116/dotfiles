#!/usr/bin/env sh

# nvm initialization is really slow so load it lazy only if needed.
# It reduces opening new tab in terminal by .5 second
#
# use it as a binary to make `/usr/bin/env node` command work. This
# is required for example for gulp which will not be aware of function

if [ -z "$NVM_DIR" ]; then
  export NVM_DIR="$HOME/.nvm"
  source $(brew --prefix nvm)/nvm.sh
fi

eval $(nvm which default) $@
